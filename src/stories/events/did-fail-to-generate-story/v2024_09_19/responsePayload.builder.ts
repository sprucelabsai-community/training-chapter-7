import { buildSchema } from '@sprucelabs/schema'

const didFailToGenerateStoryResponsePayloadBuilder = buildSchema({
    id: 'didFailToGenerateStoryResponsePayload',
    fields: {},
})

export default didFailToGenerateStoryResponsePayloadBuilder
