import { buildSchema } from '@sprucelabs/schema'

const listFamilyMembersEmitTargetBuilder = buildSchema({
    id: 'listFamilyMembersEmitTarget',
    fields: {},
})

export default listFamilyMembersEmitTargetBuilder
