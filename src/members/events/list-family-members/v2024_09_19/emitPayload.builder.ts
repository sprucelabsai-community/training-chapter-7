import { buildSchema } from '@sprucelabs/schema'

const listFamilyMembersEmitPayloadBuilder = buildSchema({
    id: 'listFamilyMembersEmitPayload',
    fields: {},
})

export default listFamilyMembersEmitPayloadBuilder
