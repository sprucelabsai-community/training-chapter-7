import { buildSchema } from '@sprucelabs/schema'

const submitFeedbackEmitTargetBuilder = buildSchema({
    id: 'submitFeedbackEmitTarget',
    fields: {},
})

export default submitFeedbackEmitTargetBuilder
